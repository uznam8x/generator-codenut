const fs = require('fs');
const path = require('path');
const _ = require('lodash');

module.exports = {
  card: {
    props: {
    },
    template: path.resolve(__dirname, './template.html'),
  }
};
